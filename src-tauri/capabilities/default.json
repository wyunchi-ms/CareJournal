{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "CareJournal desktop window with backend invoke commands only; no shell, filesystem, dialog, or HTTP plugin permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
