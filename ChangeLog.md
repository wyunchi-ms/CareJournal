# ChangeLog

## v0.7.4 (2026-07-23)

- 化疗周期叠加严格从首次给药日 Day 1 开始取数，排除 D-1、D-2、D-3 等可能属于上一周期的检查结果。
- 周期图横轴最小值固定为 D1，避免图表自动扩展后再次出现负数日期刻度。
- 增加周期 Day 1 前检查结果不进入曲线的回归测试。

## v0.7.3 (2026-07-23)

- 图表模式选择改为等宽铺满整行，移除书签按钮后不再留下无意义空白。
- 当前图表的保存状态只通过空书签和带勾书签区分，不再叠加选中背景。
- 已保存图表收拢为固定宽度入口，并改用按最近保存排序、支持即时搜索、恢复和单项删除的列表弹窗。

## v0.7.2 (2026-07-23)

- 图表保存按钮从模式选择区移动到图表卡片右上角，操作对象与位置保持一致。
- 使用书签图标区分“保存图表配置”和“置顶检查指标”，已保存配置显示激活状态，再次点击可取消保存。
- 图表卡片增加当前模式与图表标题，并补充保存按钮位置和配置内容的回归测试。

## v0.7.1 (2026-07-23)

- 移动端底部导航取消链接自身的矩形按压背景与 Android WebView 默认点击高亮，当前栏目只由暖色滑块表达。
- 保留图标和文字的即时颜色反馈，避免切页时先闪矩形、再移动滑块。

## v0.7.0 (2026-07-23)

- Android 检查报告图片改存到 App 私有文件目录，SQLite 仅保存图片路径、哈希和业务元数据；Web 端继续使用 IndexedDB 与 Base64。
- 首次启动自动把旧 SQLite Base64 图片逐条、可恢复地迁移为私有文件，写入成功后再更新记录，并在全部成功时压缩数据库。
- 迁移后的检查记录改为一次批量读取元数据，避免数百次 Capacitor 原生桥往返；遗留 Base64 行仍保留逐条读取兜底。
- OCR 队列只持久化文件引用，识别时按需读取单张图片并临时生成 Base64；报告预览直接使用本地文件地址。
- 加密备份导出时按需回填图片内容，恢复时重新写入当前设备私有目录，保持家属间分享兼容。
- 删除记录、移除 OCR 任务和恢复备份后会清理没有引用的私有图片文件。
- 增加迁移提示、原生文件持久化、按需读取、备份恢复、垃圾回收及批量记录加载的回归测试。

## v0.6.5 (2026-07-23)

- 底部导航改为单一暖色选中滑块，切换栏目时滑块沿底栏平滑移动，并兼容系统“减少动态效果”设置。
- Android 系统返回键与返回手势接入页面历史：存在上一页时返回上一页，仅在历史已经到达起点时退出 App。
- 增加底部导航滑块位置及唯一选中项的回归测试。

## v0.6.4 (2026-07-23)

- Android WebView 的默认蓝色点击高亮替换为与暖色主题一致的棕橙色按压反馈，深色模式使用对应暖色透明层。
- 移动端底部导航清除触屏设备可能残留的悬停背景，确保只显示一个当前选中栏目。
- 底部导航切页增加方向动画：向右侧栏目切换时页面从右侧进入，返回左侧栏目时从左侧进入。
- 页面动画限定为底部导航点击，内部导入页等跳转不会错误复用动画，并兼容系统“减少动态效果”设置。
- 增加底部导航动画方向的回归测试。

## v0.6.3 (2026-07-23)

- 月份切换增加方向一致的滑入动画：下个月从右侧进入，上个月从左侧进入，月份标题同步轻微移动。
- 动画保持在 240ms 内并兼容系统“减少动态效果”设置，手势和左右按钮切月均生效。
- 增加月份切换动画方向的回归测试。

## v0.6.2 (2026-07-23)

- 月历支持左滑进入下个月、右滑返回上个月；仅识别方向明确且超过阈值的横向手势，保留页面纵向滚动。
- 滑动切月后会拦截随后的误点击，避免意外选中日期或触发日历工具按钮。
- 增加左右滑动切月及纵向手势不切月的回归测试。

## v0.6.1 (2026-07-23)

- OCR 队列中的长文件名固定为单行省略显示，不再撑宽页面；桌面悬停、键盘聚焦或手机点击后可查看完整文件名。
- 补充超长文件名的可访问性与回归测试。

## v0.6.0 (2026-07-23)

- 图表页检查指标统一改为单选，实际日期趋势图只绘制一个指标；化疗周期模式继续用多条曲线叠加不同周期。
- 指标默认按肿瘤治疗常用项优先，其余按实际数值出现次数降序排列，并显示单位和出现次数。
- 增加指标置顶与手动拖拽排序，置顶顺序优先于手动顺序；同时支持键盘上下方向键排序。
- 指标顺序和置顶状态保存在本机配置中并包含在加密备份内；旧的多指标图表 Pin 兼容为使用第一个指标。
- 增加指标优先级、单选、置顶、排序及单序列图表的回归测试。

## v0.5.0 (2026-07-23)

- Android 增加系统文件夹选择，可递归扫描所选文件夹及其子文件夹中的全部报告图片，不限制数量。
- 文件夹访问权限与 OCR 队列均可跨进程重启保留，重新扫描会跳过已入库或仍在排队的文件。
- 文件夹任务只持久化轻量文件引用，轮到单张图片时才由原生端读取、纠正方向、压缩并提交 OCR，避免大量 Base64 占满内存。
- 修复数百项快速入队时旧渲染覆盖内存队列的问题，保证所有已持久化任务都能连续处理。
- 启动时批量恢复不含图片正文的轻量文件夹任务，保留 Base64 任务逐条读取保护，缩短大队列恢复时间且不牺牲内存安全。
- Web 端继续使用浏览器文件夹导入；普通拍照与选择图片入口保持不变。
- 增加 Android 文件夹导入和来源去重的回归测试。

## v0.4.0 (2026-07-21)

- 医院和科室输入支持从既有病程事件及检查记录中联想历史值，同时保留自由输入。
- 统一事件类型、检查类型、图表指标、化疗周期和重试次数的单选及多选界面。
- 将检查报告导入入口从底部导航迁移到检查记录页，底部导航精简为四项。
- 精简检查记录页，移除顶部眉题、说明、异常开关和统计卡，筛选改为搜索框右侧的图标按钮。
- 增加历史联想、统一选择控件、导航入口和精简布局的回归测试。

## v0.3.1 (2026-07-21)

- 报告详情支持从左向右滑动关闭并返回记录列表，同时保留右上角关闭入口。
- 将报告结论移动到指标和原始图片之后，并移除 OCR 识别次数及底部关闭按钮。
- 指标表移除状态列，结果列去除单位和高低箭头，单位改在指标列以小字显示。
- 偏低指标使用绿色整行背景，偏高和危急指标使用红色整行背景，正常指标保持默认背景。
- 报告原图和 OCR 导入队列图片均支持点击进入全屏放大预览。
- 删除记录改为两步确认，明确提示会同步删除关联检查事件且无法撤销。
- 增加报告表格、图片预览、删除确认和滑动关闭的回归测试。

## v0.3.0 (2026-07-21)

- 将检查记录的 Android 原生单选菜单替换为适配手机的自定义多选筛选面板。
- 增加已选类型标签、逐项移除、清除全部、类型记录数和医院原始名称展示。
- 增加检查类型归一化，将不同医院的同义报告名称合并为统一筛选类型，同时保留原名称。
- 新导入记录按归一化类型参与去重，自动创建的检查事件使用统一类型名称。
- 增加类型别名和多选筛选交互回归测试。

## v0.2.2 (2026-07-21)

- 修复 Android 冷启动时多个读取任务并发初始化 SQLite，导致已有配置和业务数据被显示为空的问题。
- 数据库初始化改为共享单一任务，所有启动读取等待同一个持久化连接完成。
- 本地数据库打开失败时不再静默进入空白界面，改为保护性错误提示和重新打开入口。
- 增加并发初始化及模拟进程重启后读取持久化配置的回归测试。

## v0.2.1 (2026-07-21)

- 将 Android 交付文件名从默认的 `android-app` 调整为可读的 `care-journal`。
- 保持手机桌面应用名称为「病程记」，并保留原 Android 包名以支持覆盖升级。

## v0.2.0 (2026-07-21)

- 检查报告导入取消文件数量限制，每张图片使用一个独立的 Azure OpenAI 请求。
- 增加持久化 OCR 后台队列、逐项与总体进度、失败重试和跨页面状态提示。
- 应用或浏览器被终止后，未完成任务可在下次打开时自动继续。
- 优化导入页在手机与 Web 端的响应式布局和无障碍状态表达。

## v0.1.0 (2026-07-20)

- 初始化 Android 与 Web 共用的 React/TypeScript 应用。
- 增加本地病程事件、检查记录、Azure OCR、趋势图与加密备份功能。
