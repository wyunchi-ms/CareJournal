# ChangeLog

## v0.19.10 (2026-07-30)

- 设备列表现在优先显示 Android"设置 → 关于本机 → 设备名"里的名字（如"小明的手机"），而不是原来的机型型号"OnePlus PGZ110"；未设置时回退到 Bluetooth 名称、最后才是厂商+机型。
- 同步状态文案跟随实际情况：只有素材真的需要传输时才会出现"正在交换图片和 PDF"；预计传输 0 个素材时直接显示"同步完成"，并且 `接收 N 个素材` 只在 N 大于 0 时才出现。
- `assetsReceived` 的语义修正：现在只统计"这次同步真正需要接收字节的新素材数"，不再把对方全量素材元数据（例如 472）当成"接收了 472 个"。
- 修复合并成功后由于后续清理（`finalizeLanAssets` / 前台服务释放）失败导致两端界面显示"同步失败"的问题——数据已经落库时不再翻红提示"失败"，改为按已合并的实际情况显示同步完成，同时保留 warning 日志。

## v0.19.9 (2026-07-30)

- 修复用户在本机清空了方案（或其他记录）的某个可选字段（例如医院、备注）后，同步会把对方设备上还保留的旧值静默复活回本机、且预览一直显示"本机 更新 1"的问题：合并算法现在把新版本的字段结构视作权威——旧版本独有的字段被视为用户在新版本里已删除，不再回填。数组仍然保留并集语义，双向新增的标签、关联 id 不会丢失。

## v0.19.8 (2026-07-30)

- 修复同步预览会在**已经持有较新版本**的一端也误报"更新 N"的问题：预览现在会完整地走一遍实际的合并判决（时间戳比较 + deepUnion + 与本地对比），只有本地行真的会被写才计入"更新"。之前本机刚改过一条方案再去同步，本机自己也被显示"更新 1"，实际数据库并不会被改。
- 预览的"新增"计数也开始遵循墓碑规则：对方发来的行如果已经被墓碑覆盖会被合并跳过，预览也就不再误报为"新增"。

## v0.19.7 (2026-07-30)

- 素材（图片、PDF）的 `updatedAt` 不再因为启动时补齐视觉指纹等派生元数据被本地时间戳覆盖：素材是内容不可变的，其 `id` 已经是 `sha256:...`，`updatedAt` 对素材没有语义。
- 启动时一次性把每条素材的 `updatedAt` 归一为 `createdAt`，清理旧版本残留的漂移数据。幂等，无需迁移版本号，也不需要用户手动处理任何东西。

## v0.19.6 (2026-07-30)

- 修复原始素材设备（老机）被反复判定为"更新 N 素材"并被覆盖的问题：素材以内容为身份（`id` 就是文件的 sha256），两端只要 `id` 一致就视为完全一致，本地的 `updatedAt / createdAt / visualFingerprint` 等元数据永不因同步被覆盖。
- 素材不再参与"更新"合并——预览仅显示"新增"和"删除"两类，避免多次同步产生的时间戳、感知指纹等本地差异反复出现在预览里。
- 记录、方案、检查、图表、报销、事件仍按原有的时间戳比较与深合并逻辑处理，用户实际编辑过的变化不受影响。

## v0.19.5 (2026-07-30)

- 同步预览新增按类别的复选框：本机将发生、对方将发生两栏中的病程 / 方案 / 检查 / 图表 / 报销 / 素材可分别勾选，默认全选。取消勾选后该类别在本次同步中会被完全跳过——不发也不收，包括对应的删除墓碑。
- 发起方通过新增的 `wantedKinds` 元数据字段告诉对端"只需要哪几类"；对端据此裁剪出站快照与素材分块，避免发起方不想要的字节仍占用带宽。旧版本对端不识别该字段时行为退化为原有的全量同步，接收侧仍会丢弃勾选未启用的内容。
- 素材未勾选但记录已勾选时，接收侧的记录仍能引用未同步的素材：这些素材落成 pendingSync 骨架行，下次同步补齐；无需强制勾选素材才能推送记录。

## v0.19.4 (2026-07-30)

- 修复局域网同步中断后，接收端会把对方的素材元数据当作"已同步"、导致下次同步预览显示无变化但实际仍需重新传输的问题：素材元数据行改为标记 pendingSync，只有真正收到分块字节后才转为可用；预览与设备广告仅暴露真正持有的素材。

## v0.19.3 (2026-07-30)

- 局域网素材接收改为逐文件落盘并单条写入素材索引，全部传完后才统一刷新目录和清理文件，避免数百份素材反复执行全库合并造成每张等待数分钟。
- 局域网同步改为可信 Wi-Fi 内直接传输，移除素材分块的压缩与加解密；分块提升至 256 KiB、结果轮询缩短至 25ms。
- 接收端与发送端都显示从记录合并到素材分块的整体进度条，并持续显示当前素材序号与百分比。
- 设备在线保留时间延长至 90 秒，广播发现增加单播回应，同步 HTTP 握手也会补全接收端设备列表。
- 文件夹导入时立即复制素材到应用私有目录；已经失效且没有私有副本的旧素材会跳过并统计，不再中止整个同步。
- 正式合并前新增双向变化预览：分别列出本机和对方将新增、更新或删除的病程、方案、检查、图表、报销与素材，发起方二次确认后才写入数据。
- 关闭局域网同步按钮改用电源图标，避免与“网络未连接”状态混淆。
- 同步预览文案改用普通用户更易理解的“同步内容”，并在等待对方返回预览期间于弹窗内显示加载动画和明确的等待提示。

## v0.19.2 (2026-07-30)

- 修复 Android 在开始传输第一个图片或 PDF 时可能失败的问题：素材分块缩小到适合 WebView 原生桥的安全尺寸，同时保持原有单文件容量上限。
- 局域网同步会话提升为应用级常驻，离开设置页不再停止设备监听或中断正在进行的同步。
- Android 传输期间启用前台数据同步服务与 CPU/Wi-Fi 唤醒锁，切到后台后继续交换记录和素材，并在传输结束后自动释放。
- 设置页主要 Card 在手机、平板和网页上始终保持一行一个，不再因宽屏布局变成 2×2。

## v0.19.1 (2026-07-29)

- 修复两台 Android 设备在部分厂商 Wi-Fi 实现下只能单向发现的问题：组播、全局广播和子网定向广播改为独立发送，单一路径失败不再阻断其他发现方式。
- 局域网同步改为先合并结构化记录、再以小块双向传输图片和 PDF，显著降低 Base64、压缩、加密与原生桥接叠加造成的内存峰值和崩溃风险。
- 同步过程中显示记录交换、素材序号和等待对方素材等阶段进度，接收设备也会显示实时处理状态。
- 通用的 CareJournal 设备名会由 Android 厂商与机型名替换，并增加原生设备列表轮询以修复偶发的事件回调丢失。
- 局域网传输协议标识升级为 v3，避免新旧版本把分块素材误当成旧式整库包；参与同步的设备需都升级到 0.19.1。

## v0.19.0 (2026-07-29)

- 新增 HarmonyOS NEXT Stage 模型工程，以 ArkWeb 复用现有响应式 React 界面，并支持手机、平板和 2in1 设备。
- 新增统一鸿蒙 JSBridge：关系型数据落入 ArkData RDB，图片与 PDF 落入应用私有素材目录，LLM 请求由 ArkTS 原生 HTTP 发出。
- 鸿蒙局域网同步实现与 Android/Web 相同的加密同步协议，并将设备发现扩展为组播与局域网广播双通道，三个端可互相发现和双向同步。
- 鸿蒙接入系统文件选择与保存、应用返回键和前后台事件，图片/PDF 预览继续使用统一素材模型。
- 鸿蒙素材仍使用现有统一资产模型和可携带同步格式，Android/Web 旧数据无需改写即可迁移，LLM 配置继续保持设备本地。
- 新增 `npm run harmony:sync` 同步共享前端资源，并新增 `npm run harmony:build` 调用 DevEco Studio 工具链生成 HAP。

## v0.18.4 (2026-07-29)

- 月历周行高度改为随该周最多事件标签数逐级增长：无标签周保持紧凑，1～4 条标签依次增加高度。
- 压缩日期与事件标签区域的上下留白，四条标签时不再保留大块空白，同时保持同一周七个日期格等高对齐。

## v0.18.3 (2026-07-29)

- 月历每天最多展示 4 条事件标签，并继续优先展示不同事件类型。
- 设置页的智能识别、局域网同步、隐私与数据、显示四个主要 Card 统一支持折叠且默认收起；收起时保留关键状态摘要。
- 检查记录编辑和详情中的日期文案统一简化为“检查日期”，底层仍按报告的采样或实际检查日期保存。

## v0.18.2 (2026-07-29)

- 原始文件去重改为真实文件字节哈希，不再受文件名影响，也修正旧版使用 Data URL 字符串计算哈希造成的身份不一致。
- 图片新增低分辨率视觉指纹；同一记录或同一报销材料中被轻微重编码的同图副本可安全识别，PDF 仍仅按精确内容去重。
- 首次启动自动补齐旧素材视觉指纹、清理重复引用及无人引用的素材文件，后续导入会在写入时直接阻止重复。

## v0.18.1 (2026-07-29)

- 设置页移除“本地数据与家属共享”以及加密备份导入、导出和全局素材去重入口，统一使用局域网同步进行设备间数据共享。
- “智能识别服务”改为默认收起的可访问折叠 Card，摘要显示当前服务商和配置状态；从导入页的配置引导进入时会自动展开。
- README、应用内隐私说明和隐私文档同步移除已下线的备份流程描述。

## v0.18.0 (2026-07-29)

- 病程新增结构化“身体记录”，支持身高、体重、体温、血压、心率和血氧；旧病程数据无需转换即可继续使用。
- 新增跨日期范围的“治疗日记”，可按治疗阶段记录反应和补充备注，方便后续在病程中连续回顾。
- 月历由纯色圆点改为紧凑的事件类型文字标签；每天最多展示 5 条，并优先展示不同事件类型，再补充同类型事件。
- 日历标签显示上限集中在 `CALENDAR_EVENT_LABEL_LIMIT` 常量中，后续可直接通过代码调整，无需增加用户设置。

## v0.17.1 (2026-07-29)

- 检查记录筛选扩展为“检查类型 + 医院”双维度多选，同一维度取并集、不同维度取交集，并在列表上方展示可单独移除的当前筛选条件。
- 重构长选项弹层为固定标题、独立滚动列表和固定操作栏，修复 Android 上列表内容从底部“完成”栏下方漏出的显示问题。

## v0.17.0 (2026-07-29)

- 智能识别新增 Azure OpenAI、OpenAI、DeepSeek、Kimi、豆包、Qwen、Gemini、MiniMax、GLM、OpenRouter 和自定义 OpenAI 兼容服务，每个服务商在本机独立保存一个模型配置。
- 设置页采用简化的“服务商—配置”二级结构：桌面和平板显示服务商列表与配置详情，手机使用单一服务商选择器，不暴露多模型管理等复杂功能。
- Azure OpenAI 升级到 `/openai/v1` 接口，移除 API Version；已有 Endpoint、Deployment、API Key 和重试次数会自动迁移且不会丢失。
- OCR 结构化输出按服务商能力使用 JSON Schema、JSON Object 或提示词约束，并继续对最终结果执行本地 Schema 校验。
- Web 本地代理扩展为受限的多服务商转发，Android 继续通过原生 HTTP 直连；所有 LLM API Key 仍不进入备份或局域网同步。

## v0.16.3 (2026-07-29)

- 趋势图和周期对比图的纵坐标改为根据当前横轴窗口内的可见数据动态计算，窗口外的历史极值不再压缩当前曲线。

## v0.16.2 (2026-07-29)

- 图表 Tooltip 限制在各自画布内，并在打开弹层、操作其他组件、滚动页面或窗口失焦时统一清理，避免旧提示框覆盖新界面。
- 长时间趋势详情默认聚焦最近六个月，新增三个月、六个月、一年和全部范围快捷切换。
- 放大时间轴缩放条与拖动手柄，支持图表内左右平移、滚轮缩放和底部手柄精细调整。

## v0.16.1 (2026-07-29)

- 收藏图表卡片改为详情入口：周期对比图可勾选并自动保存默认显示曲线，同时展示当前图表涉及的检查报告。
- 图表详情的报告摘要复用检查页样式，点击可直接打开对应检查报告；手机、平板和网页端按内容宽度自适应排版。
- “浏览其他图表”改为按需添加流程：已有收藏时默认收起创建器，点击“添加图表”后再选择模式、指标和默认周期。
- 移除图表页重复的“收藏图表”可见标题，顶部只保留添加和管理操作。

## v0.16.0 (2026-07-29)

- 图表页默认一次展示全部收藏图表：手机纵向排列，平板和网页自动切换为多列响应式网格。
- 图表触控采用方向分工：左右滑动调整时间轴，上下滑动交给页面滚动，避免 Canvas 截断纵向浏览。
- 报销计划默认即为待报销，不显示状态标签或特殊背景；第一次左滑可直接标记已报销，且只有已报销状态使用绿色强调。

## v0.15.1 (2026-07-29)

- 检查记录日期统一为明确的“采样／检查日期”：化验和病理只取标本采集日期，无标本检查取实际检查／执行日期。
- Azure OpenAI 识别规则明确忽略申请、开单、送检、审核、报告和打印日期；无法识别时保留空值，不再自动使用当天日期。
- 数据库、旧备份和局域网同步数据会自动把原 `examDate` 迁移为 `sampleDate`，并移除容易混淆的 `reportDate` 字段。

## v0.15.0 (2026-07-29)

- 局域网同步取消手工配对码，发现同一 Wi-Fi 下的设备后可直接选择并确认双向同步。
- 手机与网页端改用临时 ECDH 设备密钥自动协商 AES-GCM 加密，病程数据不会以明文在局域网中传输。
- 同步确认页、设置说明和隐私说明移除配对码流程，并继续排除 Azure OpenAI 配置与 OCR 队列。

## v0.14.0 (2026-07-28)

- 新增可复用的滑动列表项组件，统一左滑露出删除、标记、重试、复制等单项操作，并支持键盘方向键展开和收起。
- 检查记录、病程日历、识别队列、报销计划、报销附件、治疗方案、检查指标和已保存图表接入统一列表交互。
- 支持长按检查记录、病程事件、识别任务和报销计划进入批量选择，长按治疗方案或检查指标进入排序模式。
- 编辑模式统一提供全选、批量删除或排序完成入口；危险删除仍通过底部确认框二次确认。

## v0.13.2 (2026-07-28)

- 加大报销材料标题卡与列表之间的留白，同时收窄报销计划卡片之间的间距。
- 报销计划默认隐藏“已报销”和删除操作，向左滑动卡片后从右侧露出操作按钮。
- 支持使用键盘左右方向键展开和收起滑动操作，并继续通过底部确认框防止误删。

## v0.13.1 (2026-07-28)

- 空报销页仅保留空状态中的创建入口，隐藏顶部重复的新建按钮，并进一步压缩顶部卡片高度。
- 拉开报销材料卡片间距，重新整理报销计划列表的信息、进度、已报销状态和删除操作布局。
- 住院计划不再混入门诊病历，门诊计划不再混入住院病历；住院期间的治疗与检查仍归入住院计划。
- 材料导入按钮改用明确的文件导入图标，避免与导出操作混淆。
- 报销材料中的图片改为缩略图，点击可进入支持缩放和拖动的预览。

## v0.13.0 (2026-07-28)

- 报销计划默认恢复浏览模式，长按计划后才进入多选导出模式，并提供“完成”退出入口。
- 精简报销页顶部卡片和新建按钮，减少手机首屏占用。
- 报销计划支持标记“已报销”，在列表中持续显示完成状态，避免重复申请。
- 创建住院报销计划时，自动归并住院日期范围内的化疗、检查和其他治疗事件，并合并相应材料类型与检查图片。
- 已被住院覆盖的期间事件不再单独出现在“关联病程事件”列表中。

## v0.12.3 (2026-07-28)

- 修复报销材料卡片标题被挤成竖排的问题，恢复勾选控件与材料名称的正常横向布局。
- 报销清单仅保留医院出具的病历、处方、报告、票据等材料，移除身份证明、医保手工表、商保申请书和银行卡信息。
- 已有报销计划和旧备份中的上述非医院材料会在读取时自动清理。

## v0.12.2 (2026-07-28)

- 右侧日期快速索引的滑动提示补充年份，统一显示完整的 `YYYY/MM/DD` 日期。

## v0.12.1 (2026-07-28)

- 检查记录的日期分组标题改为完整年月日，滚动到列表中部时也能直接确认年份和月份。
- 缩短右侧日期快速索引并避让底部导航，最多显示 24 个等距刻度，长记录列表不再延伸到屏幕下端。

## v0.12.0 (2026-07-27)

- 检查记录改为按年、月、日分组，右侧新增可拖动和键盘操作的日期快速索引。
- 新增报销材料页面，可从住院、门诊、检查、放疗、化疗等病程事件生成对应材料待办。
- 创建计划时会自动匹配关联或同一就诊时段的检查图片，并支持重新匹配、手动勾选及自定义补充材料。
- 支持基本医保、商业保险、医保加商保、自费和国际医疗／除外责任等报销类型。
- 每类材料支持多张图片或多个 PDF，可从本地导入或调用相机拍摄。
- 支持勾选多个报销计划导出本地 ZIP，按病程事件和材料类型分文件夹整理；未补材料会生成占位说明。
- 报销计划及其附件纳入本地数据库、原生文件回收保护和加密备份恢复。

## v0.11.30 (2026-07-27)

- 新增统一的底部确认面板，病程事件、检查记录、治疗方案和已保存图表的删除均使用同一交互。
- 移除浏览器系统确认框；备份恢复等覆盖本机数据的高风险操作也改用底部确认面板。
- 统一危险操作的警示说明、取消/确认按钮、处理中状态和错误提示。

## v0.11.29 (2026-07-27)

- 检查指标选择器默认改为浏览模式，不再直接显示拖动手柄；长按指标后才进入排序模式。
- 置顶与取消置顶在浏览和排序模式下均可直接操作，并为排序模式增加“完成”出口。
- 新增按指标名称、代码或单位搜索，移除顶部“常用指标优先”说明卡片。

## v0.11.28 (2026-07-27)

- 修复点击图表数据点后切换检查指标，旧指标浮动提示仍残留的问题。
- 切换指标或图表模式时主动关闭提示框，同时保留图表实例及缩放状态。

## v0.11.27 (2026-07-27)

- 实际日期趋势图默认隐藏化疗、手术和住院等病程标记，点击右上角按钮后再显示。

## v0.11.26 (2026-07-27)

- 按模板创建化疗时，“创建周期数”默认改为 1，切换方案也不会自动生成多个未确定周期。
- 移除表单下方“将创建若干给药事件”的预览卡片，缩短新建化疗表单。

## v0.11.25 (2026-07-27)

- 实际日期趋势图右上角新增病程标记显示/隐藏开关。
- 隐藏后仅移除化疗、手术和住院事件竖线及文字，指标曲线、数据点和缩放条保持显示。

## v0.11.24 (2026-07-27)

- 化疗周期图改用 12 色高区分度色板，超过 12 个周期后继续生成新颜色，不再循环复用固定 6 色。
- 颜色按周期时间序号稳定分配，增减所选周期时其他曲线不会改变颜色。

## v0.11.23 (2026-07-27)

- 化疗周期图的可见图例统一只显示 `C1、C2…`，不再附带日期。
- 移除横坐标“疗程（天）”标题，仅保留紧凑的数字刻度。
- 图例取消分页和翻页箭头，全部周期按手机宽度自动换行，并为新增行预留绘图区空间。

## v0.11.22 (2026-07-27)

- “叠加周期”选择列表固定按 Day 1 从近到远排列，不再受通用最近使用排序影响。
- 允许不同疗程存在相同周期号；列表保留全部周期，并通过 Day 1 日期区分。
- 图表遇到重复周期号时使用 `C1·260224` 形式的紧凑日期后缀，避免重复 `C1` 被视为同一条线。

## v0.11.21 (2026-07-27)

- 化疗周期叠加图使用临床常见的 `C1、C2…` 表示各周期，压缩图例与提示框文本。
- 缩小图例符号、分页控件和周期间距，并收紧图表四周留白，让手机端一次显示更多周期。
- 手机端隐藏图表标题上方重复的模式说明，保留指标、单位、疗程日和数据值。

## v0.11.20 (2026-07-27)

- 化疗周期叠加图横坐标移除 `D` 和 `+` 前缀，改为紧凑的纯数字疗程日。
- 横坐标名称由“相对 Day 1（天）”调整为“疗程（天）”，Day 1 显示为 1。

## v0.11.19 (2026-07-27)

- 从病程日历打开检查详情时保留正在查看的月份和选中日期。
- 通过详情关闭按钮、浏览器返回或 Android 返回手势回到病程后，恢复打开检查单前的日历上下文。

## v0.11.18 (2026-07-27)

- 所有下拉列表会在当前设备保留最近使用的 5 个值，并按最近使用顺序置顶显示。
- 医院、科室、治疗方案、药物单位等同类字段可跨页面或跨表单共享最近使用记录。
- 自由输入型下拉只在选中候选或输入完成后记录，不会把输入中的临时文字加入历史。

## v0.11.17 (2026-07-24)

- 点击治疗方案列表项后先进入只读预览，不再直接打开编辑表单。
- 预览按治疗日使用紧凑表格展示药物、剂量、用法和备注，并兼容放疗及其他治疗安排。
- 预览右下角新增“编辑方案”按钮；编辑模式移除底部“取消”按钮，仍可通过弹窗关闭按钮退出。

## v0.11.16 (2026-07-24)

- 设置页“本地数据与家属共享”新增“全局图片去重”入口，可手动扫描全部检查记录。
- 去重完成后显示扫描、更新、移除重复图片及回收本地文件的数量；唯一图片不会被删除。

## v0.11.15 (2026-07-24)

- 检查详情的“重新识别”移动到底部，与“删除记录”位于同一操作栏。
- 图片保存、导入合并、备份恢复和重新识别统一按内容哈希、来源及存储身份去重。
- 应用启动时会自动清理既有检查记录中的重复图片，并回收不再引用的原生图片文件。

## v0.11.14 (2026-07-24)

- OCR 识别统一要求使用中国大陆临床检验常用单位，并将项目标准单位表直接提供给识别模型。
- 台湾等地区报告的结果值、参考上下限和参考范围会与单位同步换算，例如血红蛋白由 `g/dL` 转为 `g/L` 时数值乘以 10。
- 无法可靠确定换算关系时不猜测数值，避免出现只替换单位而数值仍保留原倍率的错误。

## v0.11.13 (2026-07-24)

- 从病程打开关联检查详情后，Android 返回手势、详情关闭按钮及右滑关闭都会回到来源病程页，不再停留在检查列表。
- 应用返回栈现在同时追踪路径和查询参数，并在浏览器历史回退时同步移除已返回的页面，避免重复返回。

## v0.11.12 (2026-07-24)

- 检查详情新增“重新识别”，可使用原始图片重新提取报告内容并就地更新当前记录。
- 重新识别保留原记录 ID、图片、创建时间和病程关联，多张报告图片会合并去重后的指标。
- Android 启动图标更新为“病历本 + 心电轨迹”图形，并补齐普通、圆形及自适应图标资源。

## v0.11.11 (2026-07-24)

- 病程日程中的关联检查事件可直接跳转到对应检查记录详情。
- 没有关联检查记录的手工事件继续打开病程事件编辑，不改变原有处理方式。

## v0.11.10 (2026-07-24)

- Android 返回手势优先关闭当前弹窗，再按应用内实际访问顺序返回上一页。
- 应用内返回不再依赖 WebView 的 `canGoBack`，没有应用内历史时才退出。

## v0.11.9 (2026-07-24)

- Android 端固定使用竖屏显示，不再跟随系统自动旋转。
- 治疗方案页面按页面编排、方案列表和编辑表单拆分组件，保持现有交互不变。

## v0.11.8 (2026-07-24)

- 移动端底部导航调整为“方案、检查、病程、图表、设置”，病程固定在最中间。
- 中间的病程图标加大并保持完整触控区域，突出最常用的病程入口。
- 页面滑动方向和底栏选中滑块同步使用新的导航顺序。

## v0.11.7 (2026-07-24)

- 单次病程事件的“治疗方案”改为可输入的模板选择框，可从方案列表选择，也可以继续自由填写病历中的其他方案名称。
- 化疗、放疗、靶向治疗和免疫治疗按事件类型只展示对应方案；普通用药事件可选择维持治疗或支持治疗方案。
- 方案下拉项显示方案类型、周期天数和模板名称，选择存在正式病历方案名的模板时优先写入正式名称。
- 医院、科室等既有历史联想输入保持原有行为和视觉不变。

## v0.11.6 (2026-07-24)

- Android 拖拽生命周期改为由窗口按 pointerId 全程追踪，不再依赖会随列表换位的拖拽把手 DOM。
- 移除“指针捕获丢失即结束拖动”的处理；向下换位导致 WebView 重排 DOM 时不会再被误判为用户松手。
- 拖动现在只在真实 pointerup 或系统 pointercancel 时结束，连续向下跨越多行不会中断。

## v0.11.5 (2026-07-24)

- 治疗方案拖拽改为按其他卡片的中心线计算插入位置，忽略当前卡片的占位区域，消除向下移动时每跨一行都要停顿的问题。
- 向上和向下拖动使用完全对称的换位规则，快速跨越多行时可直接落到对应位置。
- 浮空卡片限制在方案列表的顶部和底部边界内；拖到边缘仍可排序到第一项或最后一项，但卡片不会越出列表。

## v0.11.4 (2026-07-24)

- 治疗方案浮空卡片改为每帧直接更新 GPU 位移，不再在每个拖动事件中触发整张 React 列表重渲染，快速移动时跟手更稳定。
- 松手、系统取消触摸或指针捕获丢失时统一立即隐藏浮层并结束拖动，避免浮层与目标卡片短暂重叠或停留。
- 快速连续换位时会取消上一段未完成的让位动画，并从卡片当前视觉位置继续过渡，减少动画叠加造成的跳动。

## v0.11.3 (2026-07-23)

- 治疗方案卡片移除重复的编辑图标，点击卡片内容即可进入编辑页面。
- 复制和删除固定在卡片右上角并保持单行排列，方案卡不再因操作按钮换行而产生大块空白。
- 长按模式继续只用于拖拽排序；删除保留为可见的红色操作，并继续要求二次确认。

## v0.11.2 (2026-07-23)

- 拖动治疗方案时显示跟随手指移动的浮空卡片，增加暖色描边、抬升阴影和轻微缩放，明确当前正在移动的方案。
- 原位置保留淡化虚线占位，其他方案在换位时平滑让位，松手后恢复正常列表。
- 排序动效兼容系统“减少动态效果”设置，并保留键盘方向键排序能力。
- Android Debug 构建固定使用项目已有签名，避免本机调试环境变化后无法覆盖安装，同时保留手机中的本地数据。

## v0.11.1 (2026-07-23)

- 治疗方案排序模式改为左侧拖拽把手与方案内容同一行，手机端不再把六点把手挤到下一行，列表项高度恢复紧凑。
- 桌面与移动端统一采用“把手列 + 内容列”布局，拖动提示同步改为左侧把手。
- 排序完成按钮改为暖色实心胶囊样式，缩小视觉体积并保留清晰的完成图标、文字和触控区域。

## v0.11.0 (2026-07-23)

- 放疗方案的每个治疗日增加结构化“放疗部位”和“单次剂量”字段，剂量固定以 Gy 记录；保存前会校验每一天的部位和正数剂量。
- 放疗日标题直接汇总显示“部位 · 剂量 Gy”，复制前一天会同时复制部位、剂量和备注。
- 治疗方案列表支持长按进入排序模式，通过右侧拖拽把手调整顺序，点击完成后将顺序持久化到 SQLite 或 IndexedDB。
- 排序把手同时支持键盘上下方向键，排序模式提供明确标题和完成按钮，避免普通浏览时误触。
- OCR 系统提示词和 JSON 字段说明强化单位核对：数值、参考范围、单位和倍率必须按图片原文成组提取，禁止单位换算、移动小数点、乘除 10 或猜测单位。
- 特别提醒模型关注台湾与大陆报告常见的 g/dL、g/L、10^3/μL、10^9/L、10^4/μL、10^12/L 等基础单位和倍率差异。
- 增加放疗字段复制、长按排序、顺序持久化和 OCR 单位约束的回归测试。

## v0.10.0 (2026-07-23)

- “化疗方案”泛化为“治疗方案”，新建时可选择化疗、放疗、维持治疗、靶向治疗、免疫治疗、支持治疗或其他方案；旧模板自动按化疗方案兼容。
- 化疗、维持、靶向、免疫和支持治疗继续使用逐日用药表；放疗和其他方案改为逐日填写治疗安排，不再强制填写药物。
- 治疗方案列表增加类型标签，并按方案类型显示用药条数或安排数量。
- 移除治疗方案页面最外层的大卡片，让标题、操作入口和方案列表直接进入页面内容区。
- 删除治疗方案统一使用不可撤销提示的二次确认弹窗，取消不会删除，确认后才写入本地数据库。
- 病程页的化疗周期创建器只显示化疗类型模板，避免放疗、维持等其他方案混入化疗模板列表。
- 增加放疗方案保存、类型标签、删除确认和模板类型隔离的回归测试。

## v0.9.3 (2026-07-23)

- 每个给药日改为无外层圆角框的扁平折叠区，只保留清晰的日期标题和分隔线，减少多层卡片造成的视觉臃肿。
- D1 默认展开，其余日期默认收起；每日可单独展开或折叠，复制前一天后会自动展开目标日期，便于立即核对和调整。
- 化疗剂量单位改用 App 统一的选择面板：输入区只显示简短单位，选择面板补充按体表面积、按体重、总剂量和目标暴露量等说明，并支持清除单位及保留旧记录单位。
- 化疗方案表单和每日药物行的输入框高度、内边距与单位按钮均缩小，手机端继续保留足够的可点击区域。
- 更新每日折叠、复制展开和剂量单位选择的回归测试。

## v0.9.2 (2026-07-23)

- D1、D2、D3 等每日方案改为带暖色编号徽标和左侧强调线的轻量卡片，增强日期分组辨识度但不恢复厚重的多层卡片。
- 手机端药物名称改为标签与输入框同行，剂量改为“标签 + 数值 + 单位”同行，进一步减少纵向高度。
- 化疗剂量单位改为下拉选择，预置按体表面积、按体重、固定总剂量、IU 和 AUC 等常用表达，并保留旧记录中的非预置单位。
- AUC 剂量在生成病程事件时按“AUC 5”等临床常用顺序显示，其他单位继续按“100 mg/m²”等方式汇总。
- 增加剂量单位选项和 AUC 文本汇总的回归测试。

## v0.9.1 (2026-07-23)

- 化疗方案改为独立的底部“方案”导航页面，不再占用日历工具栏空间，避免与“今天”、事件筛选和新建事件互相挤压。
- 底部导航扩展为病程、检查、方案、图表、设置五项，并同步调整当前页面滑块和左右页面切换动画。
- 手机端每日用药改为紧凑布局：默认显示药物名称、剂量和单位，用法/途径与备注按需展开。
- 周期日改为小尺寸行内输入，D1/D2/D3 外层改用轻量分隔线，减少多层圆角卡片造成的臃肿感。
- 保留桌面端完整表格、44px 触控区域和无障碍标签，并增加独立方案导航与详情展开状态的回归测试。

## v0.9.0 (2026-07-23)

- 化疗方案管理从设置页迁移到独立页面，并在病程日历工具栏增加快捷入口；底部导航仍保持病程选中，页面内可直接返回病程。
- 每个给药日改为结构化用药表，每行独立记录药物名称、剂量、单位、用法或给药途径和备注，支持新增及删除药物。
- 手机端用药表自动改为纵向药物卡片，桌面宽屏显示为紧凑表格，避免小屏横向滚动。
- “复制前一天”会复制前一天的全部药物行和当日备注，并为复制项生成独立 ID，后续修改不会影响原日期。
- 旧版模板中的整段药物和剂量会自动转换为兼容用药行；生成病程事件时将结构化药物汇总为易读文本，现有日历与备份格式继续兼容。
- 增加逐药剂量汇总、多药复制、独立每日用药以及病程页方案入口的回归测试。

## v0.8.1 (2026-07-23)

- 化疗模板改为按本周期给药天数生成 D1、D2、D3 等每日用药卡，每天可独立填写药物、剂量与用法和当日备注。
- 每日用药卡支持一键复制前一天内容，复制药物、剂量和备注但保留目标周期日，适合大部分相同、个别天加药的方案。
- 每张卡的周期日仍可单独修改，因此继续支持 D1、D8 等非连续给药安排。
- 旧模板会按原给药日自动转换为每日计划，并把旧的共用药物和剂量复制到每一天，不需要手工迁移。
- 批量创建事件时使用对应日期自己的药物和剂量，同时保留既有周期归组与灵活顺延逻辑。
- 增加不同日期独立用药、旧模板兼容、连续日期生成和复制前一天的回归测试。

## v0.8.0 (2026-07-23)

- 增加本地化疗方案模板，可维护模板名称、周期天数、D1/D8 等给药日、默认周期数、方案药物剂量、医院、科室和备注。
- 新建化疗事件时可套用模板，输入首周期 Day 1、起始周期编号和周期数后批量生成每个给药日的日历事件。
- 模板只负责生成初始计划；已生成事件可以单独调整日期，也可将相同天数应用到本周期后续给药或后续所有周期，适应血象不合适导致的延期。
- 每个生成事件保留最初计划日期，并按疗程与周期归组；D1 调整后自动更新周期 Day 1，同周期 D1/D8 在周期图中仍只显示为一条曲线。
- 化疗模板纳入 SQLite/IndexedDB 本地持久化、医院科室动态词表以及加密导入导出，删除模板不会删除已经生成的病程事件。
- 公共 ID 生成器增加安全随机降级，兼容不提供 `crypto.randomUUID()` 的局域网 HTTP 调试环境。
- 增加化疗模板生成、灵活顺延、周期归组和移动端创建流程的回归测试。

## v0.7.4 (2026-07-23)

- 化疗周期叠加严格从首次给药日 Day 1 开始取数，排除 D-1、D-2、D-3 等可能属于上一周期的检查结果。
- 周期图横轴最小值固定为 D1，避免图表自动扩展后再次出现负数日期刻度。
- 增加周期 Day 1 前检查结果不进入曲线的回归测试。

## v0.7.3 (2026-07-23)

- 图表模式选择改为等宽铺满整行，移除书签按钮后不再留下无意义空白。
- 当前图表的保存状态只通过空书签和带勾书签区分，不再叠加选中背景。
- 已保存图表收拢为固定宽度入口，并改用按最近保存排序、支持即时搜索、恢复和单项删除的列表弹窗。

## v0.7.2 (2026-07-23)

- 图表保存按钮从模式选择区移动到图表卡片右上角，操作对象与位置保持一致。
- 使用书签图标区分“保存图表配置”和“置顶检查指标”，已保存配置显示激活状态，再次点击可取消保存。
- 图表卡片增加当前模式与图表标题，并补充保存按钮位置和配置内容的回归测试。

## v0.7.1 (2026-07-23)

- 移动端底部导航取消链接自身的矩形按压背景与 Android WebView 默认点击高亮，当前栏目只由暖色滑块表达。
- 保留图标和文字的即时颜色反馈，避免切页时先闪矩形、再移动滑块。

## v0.7.0 (2026-07-23)

- Android 检查报告图片改存到 App 私有文件目录，SQLite 仅保存图片路径、哈希和业务元数据；Web 端继续使用 IndexedDB 与 Base64。
- 首次启动自动把旧 SQLite Base64 图片逐条、可恢复地迁移为私有文件，写入成功后再更新记录，并在全部成功时压缩数据库。
- 迁移后的检查记录改为一次批量读取元数据，避免数百次 Capacitor 原生桥往返；遗留 Base64 行仍保留逐条读取兜底。
- OCR 队列只持久化文件引用，识别时按需读取单张图片并临时生成 Base64；报告预览直接使用本地文件地址。
- 加密备份导出时按需回填图片内容，恢复时重新写入当前设备私有目录，保持家属间分享兼容。
- 删除记录、移除 OCR 任务和恢复备份后会清理没有引用的私有图片文件。
- 增加迁移提示、原生文件持久化、按需读取、备份恢复、垃圾回收及批量记录加载的回归测试。

## v0.6.5 (2026-07-23)

- 底部导航改为单一暖色选中滑块，切换栏目时滑块沿底栏平滑移动，并兼容系统“减少动态效果”设置。
- Android 系统返回键与返回手势接入页面历史：存在上一页时返回上一页，仅在历史已经到达起点时退出 App。
- 增加底部导航滑块位置及唯一选中项的回归测试。

## v0.6.4 (2026-07-23)

- Android WebView 的默认蓝色点击高亮替换为与暖色主题一致的棕橙色按压反馈，深色模式使用对应暖色透明层。
- 移动端底部导航清除触屏设备可能残留的悬停背景，确保只显示一个当前选中栏目。
- 底部导航切页增加方向动画：向右侧栏目切换时页面从右侧进入，返回左侧栏目时从左侧进入。
- 页面动画限定为底部导航点击，内部导入页等跳转不会错误复用动画，并兼容系统“减少动态效果”设置。
- 增加底部导航动画方向的回归测试。

## v0.6.3 (2026-07-23)

- 月份切换增加方向一致的滑入动画：下个月从右侧进入，上个月从左侧进入，月份标题同步轻微移动。
- 动画保持在 240ms 内并兼容系统“减少动态效果”设置，手势和左右按钮切月均生效。
- 增加月份切换动画方向的回归测试。

## v0.6.2 (2026-07-23)

- 月历支持左滑进入下个月、右滑返回上个月；仅识别方向明确且超过阈值的横向手势，保留页面纵向滚动。
- 滑动切月后会拦截随后的误点击，避免意外选中日期或触发日历工具按钮。
- 增加左右滑动切月及纵向手势不切月的回归测试。

## v0.6.1 (2026-07-23)

- OCR 队列中的长文件名固定为单行省略显示，不再撑宽页面；桌面悬停、键盘聚焦或手机点击后可查看完整文件名。
- 补充超长文件名的可访问性与回归测试。

## v0.6.0 (2026-07-23)

- 图表页检查指标统一改为单选，实际日期趋势图只绘制一个指标；化疗周期模式继续用多条曲线叠加不同周期。
- 指标默认按肿瘤治疗常用项优先，其余按实际数值出现次数降序排列，并显示单位和出现次数。
- 增加指标置顶与手动拖拽排序，置顶顺序优先于手动顺序；同时支持键盘上下方向键排序。
- 指标顺序和置顶状态保存在本机配置中并包含在加密备份内；旧的多指标图表 Pin 兼容为使用第一个指标。
- 增加指标优先级、单选、置顶、排序及单序列图表的回归测试。

## v0.5.0 (2026-07-23)

- Android 增加系统文件夹选择，可递归扫描所选文件夹及其子文件夹中的全部报告图片，不限制数量。
- 文件夹访问权限与 OCR 队列均可跨进程重启保留，重新扫描会跳过已入库或仍在排队的文件。
- 文件夹任务只持久化轻量文件引用，轮到单张图片时才由原生端读取、纠正方向、压缩并提交 OCR，避免大量 Base64 占满内存。
- 修复数百项快速入队时旧渲染覆盖内存队列的问题，保证所有已持久化任务都能连续处理。
- 启动时批量恢复不含图片正文的轻量文件夹任务，保留 Base64 任务逐条读取保护，缩短大队列恢复时间且不牺牲内存安全。
- Web 端继续使用浏览器文件夹导入；普通拍照与选择图片入口保持不变。
- 增加 Android 文件夹导入和来源去重的回归测试。

## v0.4.0 (2026-07-21)

- 医院和科室输入支持从既有病程事件及检查记录中联想历史值，同时保留自由输入。
- 统一事件类型、检查类型、图表指标、化疗周期和重试次数的单选及多选界面。
- 将检查报告导入入口从底部导航迁移到检查记录页，底部导航精简为四项。
- 精简检查记录页，移除顶部眉题、说明、异常开关和统计卡，筛选改为搜索框右侧的图标按钮。
- 增加历史联想、统一选择控件、导航入口和精简布局的回归测试。

## v0.3.1 (2026-07-21)

- 报告详情支持从左向右滑动关闭并返回记录列表，同时保留右上角关闭入口。
- 将报告结论移动到指标和原始图片之后，并移除 OCR 识别次数及底部关闭按钮。
- 指标表移除状态列，结果列去除单位和高低箭头，单位改在指标列以小字显示。
- 偏低指标使用绿色整行背景，偏高和危急指标使用红色整行背景，正常指标保持默认背景。
- 报告原图和 OCR 导入队列图片均支持点击进入全屏放大预览。
- 删除记录改为两步确认，明确提示会同步删除关联检查事件且无法撤销。
- 增加报告表格、图片预览、删除确认和滑动关闭的回归测试。

## v0.3.0 (2026-07-21)

- 将检查记录的 Android 原生单选菜单替换为适配手机的自定义多选筛选面板。
- 增加已选类型标签、逐项移除、清除全部、类型记录数和医院原始名称展示。
- 增加检查类型归一化，将不同医院的同义报告名称合并为统一筛选类型，同时保留原名称。
- 新导入记录按归一化类型参与去重，自动创建的检查事件使用统一类型名称。
- 增加类型别名和多选筛选交互回归测试。

## v0.2.2 (2026-07-21)

- 修复 Android 冷启动时多个读取任务并发初始化 SQLite，导致已有配置和业务数据被显示为空的问题。
- 数据库初始化改为共享单一任务，所有启动读取等待同一个持久化连接完成。
- 本地数据库打开失败时不再静默进入空白界面，改为保护性错误提示和重新打开入口。
- 增加并发初始化及模拟进程重启后读取持久化配置的回归测试。

## v0.2.1 (2026-07-21)

- 将 Android 交付文件名从默认的 `android-app` 调整为可读的 `care-journal`。
- 保持手机桌面应用名称为「病程记」，并保留原 Android 包名以支持覆盖升级。

## v0.2.0 (2026-07-21)

- 检查报告导入取消文件数量限制，每张图片使用一个独立的 Azure OpenAI 请求。
- 增加持久化 OCR 后台队列、逐项与总体进度、失败重试和跨页面状态提示。
- 应用或浏览器被终止后，未完成任务可在下次打开时自动继续。
- 优化导入页在手机与 Web 端的响应式布局和无障碍状态表达。

## v0.1.0 (2026-07-20)

- 初始化 Android 与 Web 共用的 React/TypeScript 应用。
- 增加本地病程事件、检查记录、Azure OCR、趋势图与加密备份功能。
